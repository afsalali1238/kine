// Bump this when public/models assets are regenerated so browsers/proxies
// holding a stale cached copy of a previous asset are forced to re-fetch.
// (The 2,600-tri placeholder GLBs with inverted winding shipped once and were
// cached; v=2 marks the real CC0 MakeHuman rebuild.)
export const ASSET_VERSION = '2';
